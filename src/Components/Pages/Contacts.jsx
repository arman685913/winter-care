import React from 'react';
import ErrorPage from '../ErrorPage/ErrorPage';

const Contacts = () => {
    return (
        <div>
            <ErrorPage></ErrorPage>
        </div>
    );
};

export default Contacts;